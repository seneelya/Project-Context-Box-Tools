# План: универсальный ридер (реализация Vision03)

Пошаговый план превращения get_codeblock в универсальный ридер со сменными backend'ами. Идеология —
`Vision03__get_codeblock.md`. Каждая фаза самостоятельна, проверяема и коммитится отдельно.

## ⭐ СТАТУС на 2026-08-20 (снимок)

**База (ранее, закоммичено):** слой `reader/` целиком (`ir/protocol/registry/backends/classify/reader`),
приложение ходит через `Reader`, `.0` из CLI, arrow-const→landmark, ast-фолбек, фикс Windows-краша.

**Сделано ЭТОЙ сессией (закоммичено+запушено):**
- **Профили-плагины** `reader/profiles/<lang>.py` (по файлу на язык) + `presets.py` (общие наборы,
  C-подобные не копипастятся). `TreeSitterSpec` — тонкий движок над профилем.
- **`label(band)`** — filler-полоса теперь ОГЛАВЛЕНИЕ имён: `imports: a, b, …`, `assign: NAME`,
  `const:/export:/decl: …`, `+multiType` при разнотипье. Синхронно tree-sitter+ast (`reader/label.py`).
- **Огрубление импортов** — `import`+`from-import` в ОДНУ полосу (было дробление по типу узла).
- **Unified `--outline` = `.0`-at-depth**: адаптивная глубина (та же логика в core.py), landmark'и
  вглубь, filler только на уровне файла; `--dot` = тот же рендер + filler на ВСЕХ уровнях (диагностика).
- **Преамбула-склейка вернулась**: коммент над landmark вливается (граница вверх + первая содержательная
  строка в подпись). Декоративные `#===/---/___` — пропускаются (язык-независимо: строка без букв).
- **`.N`-маркер** безымянных уровней (`.2`/`.3`), на файловом уровне — голая `.` (без шума `.1`).
- **`--line` лесенкой** (staircase: номер уходит вправо с глубиной, `[s-e]` в столбец).
- **Утечка имени-токена рамки без тела** (`#ifndef NAME`, C# `qualified_name`) — отсеяна.
- **Лейблы C/C++ `declaration`** (`decl: NAME, …`).
- **Тесты на НОВОМ пути**: `check.py` ходит через `Reader` (outline=`.0`-рендер; ladder/query/levels/
  declarations делегируются). `expected.py` OUTLINE перегенерён (регресс-эталон). **Оракул 88/88.**
- Golden-эталон старых хендлеров — `test/parity/` (для будущих сверок).

**НЕ сделано (следующее, по убыванию ценности):**
- **Итерация 2** — многострочные именованные литералы → landmark (сейчас идут `assign: NAME` полосой;
  band-лейблы во многом закрыли исходную мотивацию, так что приоритет упал). СИНХРОННО в оба спека.
- **Паркинг**: C# file-scoped `namespace X;` (перекрытие диапазонов); SCSS (`~ERROR` — нужен
  `tree_sitter_scss` ИЛИ принять деградацию — решение за пользователем); preproc end-row bleed.
- **core2 docx** (Фаза 4) — заглавная цель Vision03 (не-код backend через готовые контракты).
- Аналайзеры (пласт 2): license/docstring-детектор → `Block.description`.

**Как проверить:** `python test/check.py` (оракул, новый путь) · `python get_codeblock.py --file F --outline|--dot|--line N`.

## Фаза 1 — Единый реестр (router seam) ⭐ первый, дёшево, высокий рычаг

**Что:** собрать 5 мест диспетчеризации в один источник правды.
- Сейчас: `lang_map` ×3 в `get_codeblock/core.py` (~255, 322, 359), `get_handler` в
  `handlers/__init__.py`, `_spec_for_ext` в `handlers/dot_classify.py`.
- Стало: `REGISTRY[ext] = Format(name, backend, spec)` — один модуль (напр. `handlers/registry.py`),
  остальные точки читают из него.

**Проверка:** добавить **Rust** (`tree_sitter_rust`) и/или **Go** (`tree_sitter_go`) как один
Spec-энтри каждый (наборы node-типов: fn/struct/enum/impl/mod → landmark/frame). Прогнать `.0` на
небольшой .rs/.go фикстуре в `test/topLevel/`. Оракул `test/check.py` — зелёный.

**Риск:** низкий (перенос диспетчеризации, поведение не меняется).

## Фаза 2 — Node-адаптер (backend seam)

**Что:** формализовать протокол узла, чтобы `.0` не зависел от tree-sitter-API.
- Протокол: `type`, `children` (named), `start_row`, `end_row`, `text`, `field(name)`.
- Тонкая обёртка `TSNode(node)` над tree-sitter; `dot_classify.DotClassifier` переводится на протокол
  (сейчас дёргает `.named_children`/`.start_point`/`.child_by_field_name` напрямую).

**Проверка:** вывод `.0` на всех текущих фикстурах побайтово совпадает с до-рефактором (снять
before/after, как делали для BUG_1).

**Риск:** средний (трогаем классификатор; спасает before/after-дифф).

## Фаза 3 — `.0` как штатный режим + Python-фолбек

**Что:**
- Внести `.0` в `core.py`/CLI отдельным флагом (`--dot` или влить в `--outline` v2) — сейчас
  запускается только как `python -m ...handlers.dot_classify`.
- Python: tree-sitter-python основной, stdlib `ast` — фолбек с громким `pip install
  tree_sitter_python` (Vision02).

**Проверка:** `--dot` из CLI на фикстурах; фолбек-ветка при отсутствии грамматики.

**Риск:** низкий-средний.

## Фаза 4 — core2: office (.docx/.doc) — первый НЕ-код backend

**Что:** backend, который строит `DotEntry`-дерево напрямую из документа.
- Декоратор: heading → landmark/frame (уровень = уровень заголовка), paragraph/таблица/картинка →
  filler-полоса. Книга буквально: заголовки = разделы/главы, абзацы = «вставки».
- Зависимость: `python-docx` (или аналог); подключить через реестр по `.docx`.

**Проверка:** `--outline`/`.0` на реальном .docx даёт карту разделов.

**Риск:** средний (новая зависимость + первый backend вне tree-sitter; проверяет весь шов IR).

## Фаза 5 — далее (оценено, не сейчас)

- **.pdf** (core3) — оглавление/структура; сложнее (нет явной иерархии, эвристики).
- **Картинки/OCR**, **plain-text** эвристики, архивы/SQLite (как у OMP `read`).
- Каждый — новый backend, эмитящий тот же IR.

## Ортогонально (не фаза, идёт параллельно под Spec-декораторами)

- **Итерация 2 таблицы ролей** (Vision02): именованные `const`/`type`-алиасы/arrow-компоненты →
  landmark по имени. Живёт в декораторах, ядра не касается.
- **Косметика:** имя-токен рамки (`~identifier` guard, `~qualified_name`) фильтровать; preproc
  end-row bleed.
- **SCSS:** решить — `tree_sitter_scss` вместо `tree_sitter_css` или принять деградацию (`~ERROR`).

## Порядок

1 → 2 → 3 закрывают «универсальный код-ридер на tree-sitter, легко добавлять языки».
4 → 5 открывают «универсальный ридер вообще». Иду по фазам, коммит на каждой.
