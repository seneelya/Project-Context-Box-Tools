# Tools

Each tool has a corresponding .TLDR.md with usage guidance and exposes its complete CLI interface through --help.

- codebase_import_search — finds where symbols from a target module are used across the project.
- get_codeblock — gets code block containing a line in a file, with optional query for parent blocks.
