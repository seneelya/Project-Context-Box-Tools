# ============================================================
# HUMAN TEST COMMANDS — graph_from_cards. Run from a __HQ/tools/ directory.
# These run OUTSIDE the target project, so cwd is NOT the project root:
# ALWAYS pass --project-root explicitly (never rely on cwd or --cards-dir).
# --help commands are the only exception (they touch no project).
#
# Root used below (container SRC/):
#   memohood = Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood
# ============================================================

# graph_from_cards: default map (tree view).
# expect: "# map — N modules · tree", "## _engine/ (N)" headings, hotspots/cycles slices, 0 unresolved

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood"

# graph_from_cards: FILE focus (slice around one file).
# expect: center "uses -> ..." + "used-by <- ..."; downstream & upstream lists within --hops

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --file _engine/embed.py --hops 1

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --file cli.py --hops 2

# graph_from_cards: CYCLES (circular deps, framing arrows show the loop closes).
# expect: chains like "→ _core/__init__.py → _core/registry.py → _core/legacy.py → _core/__init__.py →"

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --cycles

# graph_from_cards: VIEW = depth.
# expect: depth -> "## depth 0" (leaves) ... up toward entry points

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --view depth

# graph_from_cards: EDGES = out (only "→" lines = what it imports; no "←").
# note: 1 target -> "→ name"; ≥2 -> "→ ×N (a · b · c)".

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --edges out

# graph_from_cards: EDGES = in (only "←" lines = what imports it; reverse-dependency / blast-radius map).

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --edges in

# graph_from_cards: EDGES = inout is the default (both "→" and "←") — no command needed.

# graph_from_cards: VERBOSE 0 (modules + edges only; summaries hidden).
# expect: entries as "- <module>" with edge lines, NO " — <summary>"; header notes summaries hidden

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --verbose 0

# graph_from_cards: DISCREPANCIES (map vs reality digest).
# expect (clean project): "# discrepancies — none (map matches reality)"

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --discrepancies

# graph_from_cards: DISCREPANCIES regrouped by package (groups "## <pkg>/" with [kind] tags on each line).
# note: to see non-empty output, delete a card (orphan flips) or add a dep to an uncarded file (pending).

py graph_from_cards.py --project-root "Y:/Hermess/body/sandboxes/docker/default/workspace/SRC/memohood" --discrepancies --group-by package

# graph_from_cards: --help shows the flags (no project involved -> no --project-root).
# expect: --view, --edges, --verbose, --file, --hops, --cycles, --discrepancies, --group-by documented;
#         NO "-h, --help" line; NO --paths/flat

py graph_from_cards.py --help

# graph_from_cards: card-tool contract (Vision01__path-and-flag-conventions.md).
# expect: --project-root omitted -> "cards dir not found: <this repo's tools>/__map" (this repo's
#         own CONFIG__TOOLS.py is neutral, PROJECT_ROOT="." -> resolves to cwd here, sanity-check
#         passes trivially, but there's no __map/ in the tools repo itself)

py graph_from_cards.py
