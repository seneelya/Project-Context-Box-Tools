"""A DIFFERENT delegate_core.py in an unrelated package — same basename, must not be conflated
with self_delegate/plugin/self-delegate/delegate_core.py (REQ-003 acceptance criterion #4)."""


def pop_ack(args):
    return args
